## What and why

<!-- What changes, and what problem it solves. Link the API endpoint or the spec
     section if the change follows one. -->

## How it was verified

<!-- Which suites cover it, and anything checked by hand. `codegraph affected
     <files>` names the suites that actually cover the change. -->

- [ ] `make check` is green (cs-check · typecheck · build · tests at 100% coverage)
- [ ] Tests were written first — a failing test for a bug, a red test for new behaviour
- [ ] `make test-e2e` where the change is visible in a real browser

## Notes for the reviewer

<!-- A deliberate deviation from the architecture, a trade-off taken, or a
     follow-up left out of scope. Delete if there is none. -->
