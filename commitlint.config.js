/**
 * Conventional Commits, unmodified. The `scope` stays free-form on purpose: the
 * layers this project would enumerate (api, forms, services, repositories…) are
 * already visible in the changed paths, so a fixed enum would only be one more
 * list to keep in step with `src/`.
 */
export default { extends: ['@commitlint/config-conventional'] }
